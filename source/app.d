import std.stdio;

import preimage;

import std.algorithm;
import std.range;

void main()
{
    // writefln("Testing preimage cycles. {}", 10);
    // auto cycle = PreImageCycleImpl(
    //     /* nonce: */ 0,
    //     /* index:  */ 0,
    //     /* seeds:  */ PreImageCache(100, 20),
    //     /* preimages: */ PreImageCache(20, 1));
    // // iota(0, 20).each!(h => writeln("Height: {} preimage: {}", h, cycle(h)));
}
